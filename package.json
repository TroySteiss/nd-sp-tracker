{
  "name": "nd-sp-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Special-Projects / Capex Tracker — hosted multi-user, multi-region version",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:client": "esbuild shared/domain.ts --bundle --format=esm --outfile=public/domain.js",
    "build:server": "esbuild src/auth.ts src/contract.ts src/db.ts src/importers.ts src/migrate.ts src/routes.ts src/seed.ts src/seed-if-empty.ts src/server.ts shared/domain.ts --platform=node --format=esm --outdir=dist --outbase=. --sourcemap",
    "build": "npm run build:client && npm run build:server",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx src/migrate.ts",
    "seed": "tsx src/seed.ts",
    "migrate:prod": "node dist/src/migrate.js",
    "seed:prod": "node dist/src/seed.js",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/src/server.js",
    "test": "vitest run",
    "postinstall": "npm run build:client"
  },
  "dependencies": {
    "@types/adm-zip": "^0.5.8",
    "adm-zip": "^0.5.17",
    "connect-pg-simple": "^9.0.1",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.2",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^1.4.5-lts.1",
    "pdf-lib": "^1.17.1",
    "pg": "^8.13.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/connect-pg-simple": "^7.0.3",
    "@types/express": "^4.17.21",
    "@types/express-session": "^1.18.0",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
